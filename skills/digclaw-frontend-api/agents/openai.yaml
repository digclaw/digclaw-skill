interface:
  display_name: "DigClaw Skill"
  short_description: "Operate DigClaw through permitted frontend-backed APIs with cached login."
  default_prompt: "Use DigClaw Skill to perform the requested DigClaw page-equivalent function through direct API calls, reusing the cached login session when valid, enforcing frontend permission gates before business API calls, avoiding browser clicking unless visual UI testing is explicitly requested, and ending successful operations with practical next-action suggestions."
